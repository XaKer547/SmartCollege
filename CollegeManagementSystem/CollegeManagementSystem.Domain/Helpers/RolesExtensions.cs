using CollegeManagementSystem.Domain.Posts;
using SmartCollege.SSO.Shared;

namespace CollegeManagementSystem.Domain.Helpers
{
    public static class RolesExtensions
    {
        //public static Post[] ToEntity(this Roles[] roles)
        //{
        //    return roles.Select(r => new Post
        //    {
        //        Id = (int)r,
        //        Name = r.GetDisplayName()!
        //    }).ToArray();
        //}

        //public static Roles[] ToEnum(this Post[] posts)
        //{
        //    return posts.Select(r => (Roles)r.Id)
        //        .ToArray();
        //}
    }
}
